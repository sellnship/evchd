---
// Travelling-current divider: a hairline with a charge pulse running across it
// (the ::after animation lives in global.css). `style` lets callers tune layout.
interface Props { class?: string; style?: string; }
const { class: className, style } = Astro.props;
---
<div class:list={['divider', className]} style={style}></div>
